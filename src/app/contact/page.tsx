import { ContactPageClient } from './_client';

export const metadata = {
  title: 'Contact — Orcazo',
  description:
    'Get in touch with the Orcazo team. No account required — open to creators, partners, and anyone with a question.',
};

export default function ContactPage() {
  return <ContactPageClient />;
}
